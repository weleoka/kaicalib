package se.ltu.kaicalib.core.web.api.extended;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

/**
 * Currently not used because the function is not in the requirements.
 * TODO code this functionality in later version.
 */
@Controller
@RequestMapping(path = "view_reservations")
public class ViewReservationsController {
}
